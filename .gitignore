# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# Environment / secrets
.env

# Sybilion artifact cache (regenerable from job IDs)
cache/*.json
cache/**/*.json
# ...but the ceramics mock forecast IS the committed offline demo source — keep it.
!cache/mock_ceramics_forecast.json

# Pre-synthesised narration (regenerable: scripts/build_voiceover.py)
cache/audio/

# The scenario library (scenarios/) is intentionally COMMITTED — it holds real
# pre-fetched Sybilion forecasts that ship in a clean checkout so the demo is
# offline-reproducible (W17). Nothing above ignores it; this note records the intent.

# OS
.DS_Store
.claude/